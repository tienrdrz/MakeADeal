import React, { useState } from 'react';
import { useMutation } from '@apollo/client';

import Auth from '../utils/auth';



const Sell = () => {
    return (
        <main>
            
        </main>
    )
}

export default Sell;